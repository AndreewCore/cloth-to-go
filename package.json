{
  "name": "cloth-to-go",
  "version": "0.1.0",
  "private": true,
  "description": "Prototipo front-end de alquiler de ropa por día (demo de clase).",
  "scripts": {
    "lint": "eslint js",
    "lint:fix": "eslint js --fix",
    "format": "prettier --write \"**/*.{js,css,html,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,md}\""
  },
  "devDependencies": {
    "eslint": "^9.13.0",
    "globals": "^15.11.0",
    "prettier": "^3.3.3"
  }
}
